<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Задание 13.1: Разрешение на доступ в интернет -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Задание 16.3: Разрешения на доступ к геолокации -->

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Labs"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">

        <!-- Задание 16.1: API ключ для Google Maps -->
        <!-- ВАЖНО: Для реального приложения здесь должен быть ваш ключ из Google Cloud Console -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="YOUR_API_KEY_HERE" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Labs">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".UserListActivity"
            android:exported="false"
            android:label="Список пользователей" />

        <activity
            android:name=".FragmentContainerActivity"
            android:exported="false"
            android:label="Работа с фрагментами" />

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="Настройки" />

        <activity
            android:name=".NotesActivity"
            android:exported="false"
            android:label="Мои заметки" />

    </application>

</manifest>